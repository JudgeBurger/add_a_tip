{% extends 'base.html.twig' %}

{% block title %}Edit Tips{% endblock %}

{% block body %}
    <h1>Modifier {{ tip.name }}</h1>


    <div class="col-4">
        {{ include('tips/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="col-4">
        {{ include('tips/_delete_form.html.twig') }}
    </div>

    <div class="row d-flex justify-content-center align-items-center p-0 m-0">
        {# Boutton Enregistrer #}
        <div class="col-4">
            <button class="card-button">
                <a href="{{ button_label|default('Save') }}">
                    Enregistrer
                </a>
            </button>
        </div>
        {# Boutton Effacer #}
        <div class="col-4">
            <button class="card-button">Effacer</button>
        </div>
        <div class="col-4">
            <button class="card-button">
                <a href="{{ path('tips_index') }}">Retour</a>
            </button>
        </div>
        </div>
    </div>

{% endblock %}
