<nav class="navbar navbar-expand-lg navbar-light">
    <h3>ADD TIP</h3>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">

            <li><a class="nav-link-href" href="{{ path('home') }}">Accueil</a></li>


            {% if app.user and app.user.roles.0 is same as ('ROLE_ADMIN', 'ROLE_USER') %}
            <li><a class="nav-link-href" href="{{ path('tips_index') }}">Mes tips</a></li>
            {% endif %}

            {% if app.user and app.user.roles.0 is same as ('ROLE_ADMIN') %}
                <li><a class="nav-link-href" href="#">Recherche</a></li>
            {% endif %}

            <li><a class="nav-link-href" href="#">Contact</a></li>

            {% if app.user %}
                <li><a class="nav-link-href" href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
                <li><a class="nav-link-href" href="{{ path('app_login') }}">Login</a></li>
            {% endif %}
        </ul>
    </div>
</nav>